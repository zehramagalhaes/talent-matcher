export interface CategorizedKeywords {
  category: string;
  items: string[];
}

export interface ExperienceBridgeSuggestion {
  context: string;
  instruction: string;
  applied_experience: string;
}
