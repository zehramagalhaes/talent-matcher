# API Keys and Secrets
# These variables are required for the application to function properly
# Never commit actual keys to the repository

# Google Gemini API Key - Required for resume analysis
# Get your free API key from: https://ai.google.dev
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Fallback Gemini API Key (for redundancy)
# Used if the primary NEXT_PUBLIC_GEMINI_API_KEY fails
GEMINI_API_KEY_ALT=your_fallback_gemini_api_key_here

# Vercel Deployment Tokens (for CI/CD only - set in GitHub Secrets)
# VERCEL_TOKEN - Authentication token for Vercel API
# VERCEL_ORG_ID - Organization ID on Vercel
# VERCEL_PROJECT_ID - Project ID on Vercel
# These should NEVER be set in .env files, only in GitHub Secrets

# Environment
NODE_ENV=development
# Values: development, production, test
